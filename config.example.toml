# Configuration for the Golden Aegis MVP

# Path to the grammar definition file
grammar_file = "grammar.bnf"

[data]
file_path = "data/BTC-USD.csv"
hold_out_split = 0.3 # 30% of data is reserved for the final, unbiased test

[ga]
# Genetic Algorithm Parameters
population_size = 5000
num_generations = 200
mutation_rate = 0.25
crossover_rate = 0.9
tournament_size = 3
initial_genome_length = 100
max_program_tokens = 50
max_recursion_depth = 256
parsimony_penalty = 0.001
alpha = 0.01 # Smoothing factor for Calmar ratio
size_of_council = 10

# Walk-Forward Parameters (in days)
test_window_size = 21      # ~1 month of trading days

[metrics]
initial_cash = 10_000.0
annualization_rate = 252.0 # Assuming 252 trading days in a year
risk_free_rate = 0.02 # e.g., 2% annualized risk-free rate
bootstrap_runs = 2_000
transaction_cost_pct = 0.001 # 0.1% per side transaction cost (must be ≤ 1.0, >0.5 triggers warning)
slippage_pct = 0.001 # 0.1% per side slippage (must be ≤ 1.0, >0.5 triggers warning)
